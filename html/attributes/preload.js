/**
     * Specifies if and how the author thinks the audio/video should be loaded when 
the page loads
     * 
    */
class PRELOAD extends HTML5Attribute {
  constructor() {
    this.name = "preload";
  }
}
export default PRELOAD;
