/**
 * Specifies the types of files that the server accepts (only for type="file")
 *
 */
class ACCEPT extends HTML5Attribute {
  constructor() {
    this.name = "accept";
  }
}
export default ACCEPT;
