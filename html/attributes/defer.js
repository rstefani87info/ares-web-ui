/**
     * Specifies that the script is executed when the page has finished parsing 
(only for external scripts)
     * 
    */
class DEFER extends HTML5Attribute {
  constructor() {
    this.name = "defer";
  }
}
export default DEFER;
