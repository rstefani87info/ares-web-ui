/**
     * Specifies the target for where to open the linked document or where to 
submit the form
     * 
    */
class TARGET extends HTML5Attribute {
  constructor() {
    this.name = "target";
  }
}
export default TARGET;
