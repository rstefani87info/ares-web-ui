/**
     * Script to be run when a file is ready to start playing (when it has buffered 
enough to begin)
     * 
    */
class ONCANPLAY extends HTML5EventAttribute {
  constructor() {
    this.name = "oncanplay";
  }
}
export default ONCANPLAY;
