/**
     * Script to be run just as the file begins to load before anything is actually 
loaded
     * 
    */
class ONLOADSTART extends HTML5EventAttribute {
  constructor() {
    this.name = "onloadstart";
  }
}
export default ONLOADSTART;
