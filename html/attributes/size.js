/**
     * Specifies the width, in characters (for <input>) or specifies the number of 
visible options (for <select>)
     * 
    */
class SIZE extends HTML5Attribute {
  constructor() {
    this.name = "size";
  }
}
export default SIZE;
