/**
     * Script to be run when the media has paused but is expected to resume (like 
when the media pauses to buffer more data)
     * 
    */
class ONWAITING extends HTML5EventAttribute {
  constructor() {
    this.name = "onwaiting";
  }
}
export default ONWAITING;
