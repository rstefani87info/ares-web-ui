/**
     * Specifies that an <input> element should be pre-selected when the page loads 
(for type="checkbox" or type="radio")
     * 
    */
class CHECKED extends HTML5Attribute {
  constructor() {
    this.name = "checked";
  }
}
export default CHECKED;
