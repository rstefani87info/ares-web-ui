/**
     * Specifies that the track is to be enabled if the user's preferences do not 
indicate that another track would be more appropriate
     * 
    */
class DEFAULT extends HTML5Attribute {
  constructor() {
    this.name = "default";
  }
}
export default DEFAULT;
